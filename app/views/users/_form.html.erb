<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form">
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, required: true, minlength: 2, placeholder: 'Enter First Name ...' %>
  </div>
  <div class="field">
    <%= f.label :surname %><br>
    <%= f.text_field :surname, required: true, minlength: 2, placeholder: 'Enter Last Name ...' %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email, required: true, type: 'email', placeholder: 'Enter Email ...' %>
  </div>
    <br>
  <div class="actions">
    <%= f.submit %>
  </div></div>
<% end %>
